const apiUrl = "https://free-ap-south-1.cosmocloud.io/development/api";

export const urls = {
  listEmployees: `${apiUrl}/employees`,
  createEmployee: `${apiUrl}/employees`,
  getEmployee: `${apiUrl}/employees/<EMP_ID>`,
};
